# Supabase Migrations

Run migrations via Supabase CLI: `supabase db push`.

After Cursor opens this PR, you must manually run the SQL in the Supabase SQL editor before merging. Never edit migrations after they have been applied — create a new numbered migration file instead.
